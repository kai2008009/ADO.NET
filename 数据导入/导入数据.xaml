<Window x:Class="登录判断.导入数据"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入数据" Height="300" Width="300">
    <Grid>
        <Button x:Name="btn_insert" Content="导入数据" HorizontalAlignment="Left" Margin="43,54,0,0" VerticalAlignment="Top" Width="75" Click="btn_insert_Click"/>
        <Button Content="导入初始数据" HorizontalAlignment="Left" Margin="42,114,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click_1"/>

    </Grid>
</Window>
